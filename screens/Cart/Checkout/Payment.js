import React, {useState} from "react";import {ScrollView, Text, View, StyleSheet, Dimensions} from 'react-native';import { RadioButton } from 'react-native-paper';import {COLOR} from "../../../assets/font/color";import {RadioGroup} from "@mui/material";let {height} = Dimensions.get("window");const paymentCards = [    { name: 'Wallet', value: 1 },    { name: 'Visa', value: 2 },    { name: 'MasterCard', value: 3},    { name: 'Other', value: 4}]const PaymentScreen = (props) => {    const order = props.route.params;    const [selected, setSelected] = useState([        { name: 'Cash on Delivery', id: 1, value: 'Cash on Delivery' },        { name: 'Bank Transfer', value: 'Bank Transfer', id: 2 },        { name: 'Card Payment', value: 'Card Payment', id: 3}    ]);    function onPressRadioButton(radioArray) {        setSelected(radioArray)    }    const [card, setCard] = useState();    return (        <View>            <Text>                Choose your payment methods            </Text>            <RadioGroup radioButton={selected} onPress={onPressRadioButton} />        </View>    )}const styles = StyleSheet.create({    titlePay: {        marginTop: 30,        width: '100%',    },    paymentItem: {        borderColor: '#333',        borderWidth: 1,        padding: 15,        textAlign: "center"    },    clickerActive: {        backgroundColor: COLOR.mainColor,        color: '#fff'    }})export default PaymentScreen;